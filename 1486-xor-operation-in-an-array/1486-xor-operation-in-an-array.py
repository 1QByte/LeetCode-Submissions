class Solution:
    def xorOperation(self, n: int, start: int) -> int:
        nums = []
        result 

        for i in range(n):
            nums.append(start + 2 * 1)

        for num in nums:
            result ^= num

        return result